---
title: Discord bot tutorial + Free 24/7 hosting! - Discord.py
description: How to create a discord bot using python and host it 24/7 all for free!
date: "03-11-2023"
published: true
repository: "saharsh1223/discord-bot-tutorial"
---
# Video

<iframe
  style={{borderRadius: '8px', width: '640px', overflow: 'hidden'}}
  width='640'
  height='480'
  src='https://www.youtube.com/embed/pdlHqFcxr5Q'
  frameborder='0'
  allow='autoplay; encrypted-media'
  allowfullscreen
></iframe>

# Information

A tutorial on how to create a Discord Bot using Python and host it 24/7 for free! Made by [Saharsh1223](https://www.youtube.com/SaharshDev).

# Code

```
import discord
from discord.ext import commands

bot = commands.Bot(command_prefix='?', intents=discord.Intents.all())
bot.remove_command('help')

@bot.event
async def on_ready():
    print('Logged in as ' + bot.user.name + '!')

@bot.event
async def on_message(message):
    await bot.process_commands(message)

@bot.command(name='hello')
async def hello(ctx):
    await ctx.send('Hello There!')

@bot.command(name='ping')
async def ping(ctx):
    await ctx.send(f'Pong! ||{round(bot.latency * 1000)} ms||')

@bot.command(name='help')
async def help(ctx):
    em = discord.Embed(title='Help', description='See what all I could do for you!', color=000000)
    em.add_field(name='Commands', value='`?hello` to say hello to me!\n`?ping` to check my ping!\n`?help` to see this message!', inline=False)
    await ctx.send(embed=em)

bot.run('token goes here')

```

# Github

The repository is public if you want to fork and is available on [github!](https://github.com/Saharsh1223/discord-bot-tutorial)

{/* Comments */}